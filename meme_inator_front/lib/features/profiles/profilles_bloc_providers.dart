// feeds_bloc_providers.dart
import 'package:flutter_bloc/flutter_bloc.dart';


/// TODO: Remove this
final List<BlocProvider<dynamic>> profilesBlocProviders = [
  // This is commented out because ProfilePage now creates its own ViewModel
  // with the username parameter. Better to create ViewModel per page with username
];
